// run step 

py -m venv venv
venv\Scripts\activate

python -m pip install -r requirements.txt

uvicorn app.main:app --host 0.0.0.0 --port 8000


// ไปที่หน้า swagger เพื่อทดสอบ api login (/user/login [post]) โดยใช้ 
// username: admin และ password: admin เพื่อ get jwt token มาใช้ในการเรียก api อื่นๆ ที่ต้องการ authentication

// swagger
http://localhost:8000/docs


